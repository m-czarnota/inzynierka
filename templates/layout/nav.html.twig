<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Tetriso Ships</a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_dashboard_index') }}">
                    Home 🏠
                    {% if app.request.pathInfo == path('app_dashboard_index') %}
                        <span class="sr-only">(current)</span>
                    {% endif %}
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_game_arrange') }}">
                    Play! ▶
                    {% if path('app_game_arrange') in app.request.pathInfo %}
                        <span class="sr-only">(current)</span>
                    {% endif %}
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link disabled" href="#">
                    Tournament ⚔
{#                    {% if app.request.pathInfo == path('') %}#}
{#                        <span class="sr-only">(current)</span>#}
{#                    {% endif %}#}
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link disabled" href="#">
                    Rankings
{#                    {% if app.request.pathInfo == path('') %}#}
{#                        <span class="sr-only">(current)</span>#}
{#                    {% endif %}#}
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link disabled" href="#">
                    Account 👨
{#                    {% if app.request.pathInfo == path('') %}#}
{#                        <span class="sr-only">(current)</span>#}
{#                    {% endif %}#}
                </a>
            </li>
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">
                        Logout
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>